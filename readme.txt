1. Install required libraries: "pip3 install dash plotly pandas openpyxl requests scikit-learn" OR "pip3 install -r requirements.txt"
2. Run the attached script: "python web_app.py"
3. Navigate to the printed IP address: http://127.0.0.1:8050/

Notes: 
- 'notebooks' folder contains the scripts for processing the data
- 'data' folder contains the original dataset and all processed data files